import SwiftUI

// MARK: - Main View
struct PageBusinessProfile: View {
    var body: some View {
        VStack(spacing: 0) {
            headerSection
            scrollableSection
            footerSection
        }
        .edgesIgnoringSafeArea(.bottom)
    }

    // MARK: - Header Section
    private var headerSection: some View {
        VStack(spacing: 0) {
            HStack {
                VStack(alignment: .leading, spacing: 5) {
                    Text("Circl.")
                        .font(.largeTitle)
                        .fontWeight(.bold)
                        .foregroundColor(.white)
                    
                    Button(action: {
                        // Action for Filter
                    }) {
                        HStack {
                            Image(systemName: "slider.horizontal.3")
                                .foregroundColor(.white)
                            Text("Filter")
                                .font(.headline)
                                .foregroundColor(.white)
                        }
                    }
                }
                Spacer()
                VStack(alignment: .trailing) {
                    Image(systemName: "person.circle.fill")
                        .resizable()
                        .frame(width: 50, height: 50)
                        .foregroundColor(.white)
                        .offset(x: -28)
                    Text("Hello, Fragne")
                        .foregroundColor(.white)
                        .font(.headline)
                }
            }
            .padding(.horizontal)
        }
        .padding(.top, 15)
        .padding(.bottom, 10)
        .background(Color.fromHex("004aad"))
    }

    // MARK: - Scrollable Section
    private var scrollableSection: some View {
        ScrollView {
            VStack(spacing: 20) {
                companyOverviewSection
                aboutSection
                companyDetailsSection // New section added here
            }
            .padding()
            .background(Color(.systemGray4)) // Gray background for the scrollable section
        }
    }

    private var companyOverviewSection: some View {
        VStack(spacing: 10) {
            VStack(spacing: 10) {
                Rectangle()
                    .fill(Color.gray.opacity(0.2))
                    .frame(height: 120)
                    .overlay(
                        Text("Company Logo")
                            .foregroundColor(.gray)
                            .font(.headline)
                    )
                    .cornerRadius(10)
                
                Text("Company Name")
                    .font(.title2)
                    .fontWeight(.semibold)
                    .foregroundColor(.white)
            }
            .padding()
            .background(Color.fromHex("004aad")) // Blue background for the company overview
            .cornerRadius(10)
        }
    }

    private var aboutSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("About")
                .font(.headline)
                .foregroundColor(.black)
            
            // Display box with placeholder text
            Text("Write about the company here...")
                .frame(height: 120)
                .frame(maxWidth: .infinity)
                .padding()
                .background(Color.white)
                .cornerRadius(10)
                .overlay(
                    RoundedRectangle(cornerRadius: 10)
                        .stroke(Color.gray, lineWidth: 1)
                )
        }
        .padding()
        .background(Color.white) // White background for the about section
        .cornerRadius(10)
    }

    private var companyDetailsSection: some View {
        VStack(alignment: .leading, spacing: 1) {
            Text("Company Overview")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Industry", value: "Technology")
                detailRow(title: "Type", value: "Startup")
                detailRow(title: "Problem To Solve", value: "Improving connectivity for entrepreneurs")
                detailRow(title: "Cofounders", value: "2")
                detailRow(title: "Stage", value: "Seed")
                detailRow(title: "Revenue", value: "Pre-revenue")
                detailRow(title: "Location", value: "San Francisco, CA")
                detailRow(title: "Looking for", value: "Investors, Mentors, Partners")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white) // White background for the company details section
        .cornerRadius(10)
    }

    private func detailRow(title: String, value: String) -> some View {
        HStack(alignment: .top, spacing: 0) {
            Text("\(title): ")
                .font(.body)
                .fontWeight(.semibold)
                .foregroundColor(.primary)
            Text(value)
                .font(.body)
                .foregroundColor(.primary)
        }
    }

    // MARK: - Footer Section
    private var footerSection: some View {
        HStack(spacing: 15) {
            CustomCircleButton(iconName: "hand.raised.fill")
            CustomCircleButton(iconName: "briefcase.fill")
            CustomCircleButton(iconName: "person.3.fill")
            CustomCircleButton(iconName: "person.2.wave.2.fill")
            CustomCircleButton(iconName: "book.fill")
        }
        .padding(.vertical, 10)
        .background(Color.white)
    }
}

// MARK: - Custom Circle Button
struct CustomCircleButton5: View {
    let iconName: String

    var body: some View {
        Button(action: {
            // Button action
        }) {
            ZStack {
                Circle()
                    .fill(Color.fromHex("004aad"))
                    .frame(width: 60, height: 60)
                Image(systemName: iconName)
                    .resizable()
                    .aspectRatio(contentMode: .fit)
                    .frame(width: 30, height: 30)
                    .foregroundColor(.white)
            }
        }
    }
}

// MARK: - Color Extension
extension Color {
    static func fromHex5(_ hex: String) -> Color {
        var hexSanitized = hex.trimmingCharacters(in: .whitespacesAndNewlines)
        hexSanitized = hexSanitized.replacingOccurrences(of: "#", with: "")

        var rgb: UInt64 = 0
        Scanner(string: hexSanitized).scanHexInt64(&rgb)

        let red = Double((rgb >> 16) & 0xFF) / 255.0
        let green = Double((rgb >> 8) & 0xFF) / 255.0
        let blue = Double(rgb & 0xFF) / 255.0

        return Color(red: red, green: green, blue: blue)
    }
}

// MARK: - Preview
struct PageBusinessProfile_Previews: PreviewProvider {
    static var previews: some View {
        PageBusinessProfile()
    }
}
