import SwiftUI

// MARK: - Main View
struct PageBusinessProfile: View {
    var body: some View {
        NavigationView { // Wrap the main view in a NavigationView
            VStack(spacing: 0) {
                headerSection
                scrollableSection
                footerSection
            }
            .edgesIgnoringSafeArea(.bottom)
            .navigationBarBackButtonHidden(true)
        }
    }
    
    var headerSection: some View {
        VStack(spacing: 0) {
            HStack {
                // Left side: "Circl." text and "Filter" button
                VStack(alignment: .leading, spacing: 5) {
                    Text("Circl.")
                        .font(.largeTitle)
                        .fontWeight(.bold)
                        .foregroundColor(.white)
                    
                    Button(action: {
                        // Action for Filter
                    }) {
                        HStack {
                            Image(systemName: "slider.horizontal.3")
                                .foregroundColor(.white)
                            Text("Filter")
                                .font(.headline)
                                .foregroundColor(.white)
                        }
                    }
                }
                Spacer()

                // Right side: Profile image and "Hello, Fragne" text stacked
                VStack(alignment: .trailing, spacing: 5) {
                    // Profile Image
                    NavigationLink(destination: ProfilePage()) {
                        Image(systemName: "person.circle.fill")
                            .resizable()
                            .frame(width: 50, height: 50)
                            .foregroundColor(.white)
                    }

                    // "Hello, Fragne" text
                    Text("Hello, Fragne")
                        .foregroundColor(.white)
                        .font(.headline)
                }
            }
            .padding(.horizontal)
            .padding(.top, 15)
            .padding(.bottom, 10)
            .background(Color.fromHex("004aad")) // Blue background for the header
        }
    }

    
    // MARK: - Scrollable Section
    private var scrollableSection: some View {
        ScrollView {
            VStack(spacing: 20) {
                companyOverviewSection
                aboutSection
                companyDetailsSection
                valuesSection
                solutionSection
                businessModelSection
                teamSection
                financialsFundingSection
                lookingForSection
                contactUsSection
            }
            .padding()
            .background(Color(.systemGray4))
        }
    }
    
    // MARK: - Company Overview Section
    private var companyOverviewSection: some View {
        VStack(spacing: 10) {
            VStack(spacing: 10) {
                // Company logo area
                Rectangle()
                    .fill(Color.gray.opacity(0.2))
                    .frame(height: 120)
                    .overlay(
                        Image("companyLogo") // Replace with dynamic binding to the company logo
                            .resizable()
                            .scaledToFit()
                            .padding()
                            .foregroundColor(.gray) // Fallback gray color
                    )
                    .cornerRadius(10)
                
                // Company name with dynamic underline for link
                if let companyWebsiteURL = companyWebsiteURL, hasWebsiteLink {
                    Link("Company Name", destination: companyWebsiteURL)
                        .font(.title2)
                        .fontWeight(.semibold)
                        .foregroundColor(.white)
                        .underline()
                } else {
                    Text("Company Name")
                        .font(.title2)
                        .fontWeight(.semibold)
                        .foregroundColor(.white)
                }
            }
            .padding()
            .background(Color.fromHex("004aad"))
            .cornerRadius(10)
        }
    }
    
    // MARK: - Variables
    @State private var hasWebsiteLink: Bool = false // Dynamically determine if a website link exists
    @State private var companyWebsiteURL: URL? = nil // Replace with actual URL data binding
    
    // MARK: - About Section
    private var aboutSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("About")
                .font(.headline)
                .foregroundColor(.black)
            
            // Display box with placeholder text
            Text("Write about the company here...")
                .frame(height: 120)
                .frame(maxWidth: .infinity)
                .padding()
                .background(Color.white)
                .cornerRadius(10)
                .overlay(
                    RoundedRectangle(cornerRadius: 10)
                        .stroke(Color.gray, lineWidth: 1)
                )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Company Details Section
    private var companyDetailsSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Company Overview")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Industry", value: "Technology")
                detailRow(title: "Type", value: "Startup")
                detailRow(title: "Problem To Solve", value: "Improving connectivity for entrepreneurs")
                detailRow(title: "Cofounders", value: "2")
                detailRow(title: "Stage", value: "Seed")
                detailRow(title: "Revenue", value: "Pre-revenue")
                detailRow(title: "Location", value: "San Francisco, CA")
                detailRow(title: "Looking for", value: "Investors, Mentors, Partners")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Detail Row Helper Function
    private func detailRow(title: String, value: String) -> some View {
        HStack(alignment: .top, spacing: 0) {
            Text("\(title): ")
                .font(.body)
                .fontWeight(.semibold)
                .foregroundColor(.primary)
            Text(value)
                .font(.body)
                .foregroundColor(.primary)
        }
    }
    
    // MARK: - Values Section
    private var valuesSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Values")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Vision", value: "To revolutionize connectivity among entrepreneurs.")
                detailRow(title: "Mission", value: "To empower entrepreneurs by fostering meaningful connections.")
                detailRow(title: "Company Culture", value: "Innovative, inclusive, and results-driven.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Solution Section
    private var solutionSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Solution")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Product/Service", value: "Describe the product or service here.")
                detailRow(title: "Unique Selling Proposition", value: "Highlight what makes this unique.")
                detailRow(title: "Traction/Progress", value: "Summarize the milestones achieved.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Business Model Section
    private var businessModelSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Business Model")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Revenue Streams", value: "List the revenue streams.")
                detailRow(title: "Pricing Strategy", value: "Outline the pricing approach.")
                detailRow(title: "Sales/Revenue", value: "Summarize current sales/revenue.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Team Section
    private var teamSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("The Team")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "CoFounders", value: "List of cofounders.")
                detailRow(title: "Key Hires", value: "List of key hires.")
                detailRow(title: "Advisors/Mentors", value: "List of advisors or mentors.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Financials/Funding Section
    private var financialsFundingSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Financials/Funding")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Funding Stage", value: "Specify the funding stage.")
                detailRow(title: "Amount Raised", value: "Total amount raised.")
                detailRow(title: "Use of Funds", value: "Describe how funds are utilized.")
                detailRow(title: "Financial Projections", value: "Summarize projections.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Looking For Section
    private var lookingForSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Looking For")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                detailRow(title: "Roles Needed", value: "List roles needed.")
                detailRow(title: "Mentorship", value: "Specify areas where mentorship is needed.")
                detailRow(title: "Investment", value: "Describe investment needs.")
                detailRow(title: "Other", value: "Other needs or requirements.")
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Contact Us Section
    private var contactUsSection: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("Contact Us")
                .font(.headline)
                .fontWeight(.bold)
                .foregroundColor(.black)
            
            VStack(alignment: .leading, spacing: 8) {
                Text("John Doe")
                    .foregroundColor(Color.fromHex("004aad"))
                    .underline()
                    .onTapGesture {
                        // Navigate to John Doe's profile
                    }
            }
            .padding()
            .frame(maxWidth: .infinity)
            .background(Color.white)
            .cornerRadius(10)
            .overlay(
                RoundedRectangle(cornerRadius: 10)
                    .stroke(Color.gray, lineWidth: 1)
            )
        }
        .padding()
        .background(Color.white)
        .cornerRadius(10)
    }
    
    // MARK: - Footer Section
    private var footerSection: some View {
        HStack(spacing: 15) {
            NavigationLink(destination: PageEntrepreneurMatching()) {
                CustomCircleButton(iconName: "hand.raised.fill")
            }
            NavigationLink(destination: PageBusinessProfile()) {
                CustomCircleButton(iconName: "briefcase.fill")
            }
            NavigationLink(destination: PageForum()) {
                CustomCircleButton(iconName: "person.3.fill")
            }
            NavigationLink(destination: PageEntrepreneurResources()) {
                CustomCircleButton(iconName: "person.2.wave.2.fill")
            }
            NavigationLink(destination: PageEntrepreneurResources()) {
                CustomCircleButton(iconName: "book.fill")
            }
        }
        .padding(.vertical, 10)
        .background(Color.white)
    }
    
    // MARK: - Custom Circle Button
    struct CustomCircleButton: View {
        let iconName: String
        
        var body: some View {
            ZStack {
                Circle()
                    .fill(Color.fromHex("004aad"))
                    .frame(width: 60, height: 60)
                Image(systemName: iconName)
                    .resizable()
                    .aspectRatio(contentMode: .fit)
                    .frame(width: 30, height: 30)
                    .foregroundColor(.white)
            }
        }
    }
}

// MARK: - Color Extension
extension Color {
    static func fromHex5(_ hex: String) -> Color {
        var hexSanitized = hex.trimmingCharacters(in: .whitespacesAndNewlines)
        hexSanitized = hexSanitized.replacingOccurrences(of: "#", with: "")
        
        var rgb: UInt64 = 0
        Scanner(string: hexSanitized).scanHexInt64(&rgb)
        
        let red = Double((rgb >> 16) & 0xFF) / 255.0
        let green = Double((rgb >> 8) & 0xFF) / 255.0
        let blue = Double(rgb & 0xFF) / 255.0
        
        return Color(red: red, green: green, blue: blue)
    }
}

// MARK: - Preview
struct PageBusinessProfile_Previews: PreviewProvider {
    static var previews: some View {
        PageBusinessProfile()
    }
}
