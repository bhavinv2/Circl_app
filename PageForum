import SwiftUI

struct PageForum: View {
    var body: some View {
        VStack(spacing: 0) {
            // Header Section
            VStack(spacing: 10) {
                HStack {
                    Text("Circl.")
                        .font(.largeTitle)
                        .fontWeight(.bold)
                        .foregroundColor(.white)
                    Spacer()
                    VStack {
                        Image(systemName: "person.circle.fill")
                            .resizable()
                            .frame(width: 50, height: 50)
                            .foregroundColor(.white)
                        Text("Hello, Fragne")
                            .foregroundColor(.white)
                            .font(.headline)
                    }
                }
                .padding(.horizontal)
                
                HStack {
                    Button(action: {
                        // Action for Filter
                    }) {
                        HStack {
                            Image(systemName: "slider.horizontal.3")
                                .foregroundColor(.white)
                            Text("Filter")
                                .font(.headline)
                                .foregroundColor(.white)
                        }
                    }
                    Spacer()
                }
                .padding(.horizontal)
            }
            .padding(.top, 15)
            .padding(.bottom, 10)
            .background(Color.customHex("004aad"))
            .frame(height: 120) // Consistent with the ProfilePage header height
            
            // Scrollable Second Section
            ScrollView {
                VStack(spacing: 10) {
                    ForEach(0..<20) { index in
                        Text("Forum Post \(index + 1)")
                            .font(.system(size: 18))
                            .frame(maxWidth: .infinity)
                            .padding()
                            .background(Color.white)
                            .cornerRadius(10)
                            .shadow(radius: 2)
                    }
                }
                .padding()
            }
            .background(Color(UIColor.systemGray4))
            
            // Footer Section (Integrated from ProfilePage)
                       HStack(spacing: 15) {
                           CircleButton(iconName: "hand.raised.fill") // Handshake icon
                           CircleButton(iconName: "briefcase.fill") // Briefcase icon
                           CircleButton(iconName: "person.3.fill") // Community icon
                           CircleButton(iconName: "person.2.circle.fill") // Connection icon
                           CircleButton(iconName: "book.fill") // Book icon
                       }
                       .padding(.vertical, 10)
                       .background(Color.white) // Explicitly set footer background to white
                   }
                   .edgesIgnoringSafeArea(.bottom)
               }
           }

struct PageForum_Previews: PreviewProvider {
    static var previews: some View {
        PageForum()
    }
}
