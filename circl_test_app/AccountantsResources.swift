import SwiftUI

struct AccountantsResources: View {
    var body: some View {
        VStack {
            Text("accountants Resources Pageq")
                .font(.largeTitle)
                .fontWeight(.bold)
                .foregroundColor(.blue)
                .padding()
            
            Spacer()
        }
    }
}

struct AccountantsResources_Previews: PreviewProvider {
    static var previews: some View {
        InvestorsResources()
    }
}
